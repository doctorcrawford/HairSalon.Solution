@{
  Layout = "_Layout";

  <h1>Eau Claire's Salon</h1>

  <h3>@Html.ActionLink("Add a stylist", "Create", "Stylists")</h3>
  <h3>@Html.ActionLink("See all stylists", "Index", "Stylists")</h3>
  <h3>@Html.ActionLink("Add a client", "Create", "Clients")</h3>
  <h3>@Html.ActionLink("See all clients", "Index", "Clients")</h3>
}