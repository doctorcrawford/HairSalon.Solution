@{
  Layout = "_Layout";
}

<h3>Are you sure you want to delete @Model.Name?</h3>

@Html.DisplayNameFor(ModelExpressionProvider => model.Name): @Html.DisplayFor(ModelExpressionProvider => model.Name)
@using (Html.BeginForm())
{
  <input type="submit" value="Delete" />
}
@Html.ActionLink("Back to Stylists List", "Index")